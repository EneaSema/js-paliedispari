// console.log("ciao");

// Palidroma
// 1 Chiedere all’utente di inserire una parola
// 2 Creare una funzione per capire se la parola inserita è palindroma

const requestWordUser = "otto";

const 

function verificatedWord(requestWordUser) {
  for (let i = 0; i < requestWordUser.length; i++) {
    let currentCharacter = currentCharacter.push(requestWordUser[i]);
  }
  if (currentCharacter === requestWordUser) {
    console.log("Parola palindroma");
  } else {
    console.log("Parola non palidroma");
  }
}
